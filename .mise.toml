# https://mise.jdx.dev/configuration.html
[tools]
"aqua:evilmartians/lefthook" = "2.1.8"
"aqua:google/yamlfmt" = "0.21.0"
"aqua:koalaman/shellcheck" = "0.11.0"
"aqua:zizmorcore/zizmor" = "1.25.2"
node = "24.15.0"
"npm:prettier" = "3.8.3"
"npm:renovate" = "43.150.0"

[tasks.gen]
description = "Regenerate default.json from the preset directories"
run = "node scripts/gen-default.mjs"

[tasks.lint]
description = "Check JSON/JSON5, YAML, and shell formatting"
run = [
  "prettier --check .",
  "yamlfmt -lint",
  "git ls-files -co --exclude-standard '*.sh' | xargs -r shellcheck",
]

[tasks.validate]
description = "Validate Renovate presets against the schema"
run = "renovate-config-validator --strict"

[hooks]
postinstall = "lefthook install"
