# https://lefthook.dev/configuration/
pre-commit:
    parallel: true
    commands:
        prettier:
            glob: "*.{json,json5,md,yml,yaml}"
            run: mise exec -- prettier --check {staged_files}
        renovate-config-validator:
            glob: "*.json5"
            run: mise exec -- renovate-config-validator --strict {staged_files}
