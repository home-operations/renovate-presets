[pre-commit]
parallel = true

[pre-commit.commands.prettier]
glob = "*.{json,json5}"
run = "prettier --check {staged_files}"

[pre-commit.commands.renovate-config-validator]
glob = "*.json5"
run = "renovate-config-validator --strict {staged_files}"

[pre-commit.commands.format-yaml]
run = "yamlfmt {staged_files}"
glob = ["*.yml", "*.yaml"]
stage_fixed = true

[pre-commit.commands.shellcheck]
glob = "*.sh"
run = "shellcheck {staged_files}"

[pre-commit.commands.mise-lock]
glob = [".mise.toml"]
run = "MISE_LOCKED=0 mise lock && git add mise.lock"

[pre-commit.commands.zizmor]
glob = [".github/workflows/*.yaml", ".github/actions/**/action.yaml"]
run = "zizmor --offline {staged_files}"
